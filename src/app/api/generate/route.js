const { preloadStyle } = require("next/dist/server/app-render/entry-base")

Go to vercel.com, click "New Project" 

Import your GitHub repo 

Add your OPENAI_API_KEY in Vercel's Environment Variables settings 

Click "Deploy" 

In ~2 minutes, you get a live URL like preloadStyle-tweet-generator.vercel.app